# brum-ros-packages
> ROS packages for Brum-Bot a MIRTE derivative

The following packages include a setup instruction of their own:
 - `brum_camera`

## Development
This repository uses pre-commit, to set it up run:
```shell
pre-commit install
```
